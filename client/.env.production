# Production environment variables for Railway deployment
CI=false
GENERATE_SOURCEMAP=false
NODE_ENV=production
# Leave empty to use relative URLs (/api/*) which will use the Express proxy
# This avoids CORS issues
REACT_APP_API_URL=
